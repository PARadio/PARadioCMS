{
  <% if @currentItem %>
    "id": "<%= @currentItem.id %>",
    "position": "<%= @currentItem.position %>",
    "start_time": "<%= @currentItem.start_time.strftime("%I:%M:%S %p") %>",
    "name": "<%= @currentItem.episode.name %>",
    "description": "<%= @currentItem.episode.description %>",
    "created": "<%= @currentItem.episode.created_at %>",
    "updated": "<%= @currentItem.episode.updated_at %>"
  <% else %>
    "error": "Nothing is currently playing."
  <% end %>
}
