<%@page_title="Listen"%>
<div class="container">
  <h2>Now Playing: <% if @currentItem %><%= @currentItem.episode.name %><% else %>Nothing<% end %></h2>
  <audio controls>
    <source src="http://localhost:8000/livestream" type="audio/mpeg">
  Your browser does not support the audio element.
  </audio>
</div>
