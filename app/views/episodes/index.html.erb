<div class="episodes index">
  <h2>Episodes</h2>
  <%=link_to("Add New Episode", new_episode_path())%>
  <table class="listing table table-bordered table-striped" summary="Episode List">
    <tr class="header">
      <th>Episode Name</th>
      <th>Episode Number</th>
      <th>Stage</th>
      <th>Media File</th>
      <th>Actions</th>
    </tr>
    <% @episodes.each do |episode| %>
    <tr>
      <td><%= episode.name %></td>
      <td><%= episode.episode_number %></td>
      <td><%= episode.stage %></td>
      <td><%= episode.media_id%></td>
      <td class="actions">
        <%= link_to("Show",{:action => 'show', :id => episode.id}, :class => 'action show') %>
        <%= link_to("Edit",{:action => 'edit', :id => episode.id}, :class => 'action edit') %>
        <%= link_to("Delete", episode_path(episode), method: :delete) %>
      </td>
    </tr>
    <% end %>
  </table>
</div>
